from django.conf.urls import patterns, include, url
from . import views

# urlpatterns = patterns('',
#                        url(r'^results/$', views.ResultsView.as_view(), name='view_results'),
#                        )